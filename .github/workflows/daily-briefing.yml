name: Daily Briefing Update

on:
  # 매일 한국 시간 오전 9시 자동 실행 (UTC 00:00)
  schedule:
    - cron: '0 0 * * *'
  
  # 수동 실행 가능
  workflow_dispatch:

permissions:
  contents: write

env:
  NODE_VERSION: '20'

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Initialize database
        run: node scripts/init-db.js
      
      - name: Generate briefing data
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          echo "🤖 Generating daily briefing..."
          node scripts/generate-briefing.js
          
          # 생성된 데이터 확인
          if [ -f "public/data/briefing.json" ]; then
            echo "✅ Briefing data generated successfully"
            ls -lh public/data/briefing.json
          else
            echo "❌ Failed to generate briefing data"
            exit 1
          fi
      
      - name: Organize data files
        run: |
          # 한국 시간 기준 날짜
          DATE=$(TZ=Asia/Seoul date +%Y-%m-%d)
          YEAR=$(TZ=Asia/Seoul date +%Y)
          MONTH=$(TZ=Asia/Seoul date +%m)
          DAY=$(TZ=Asia/Seoul date +%d)
          
          echo "📅 Processing date: $DATE"
          
          # 디렉토리 생성
          mkdir -p public/data/$YEAR/$MONTH
          
          # 날짜별 파일 저장
          cp public/data/briefing.json public/data/$YEAR/$MONTH/$DAY.json
          echo "✅ Created: public/data/$YEAR/$MONTH/$DAY.json"
          
          # latest.json 업데이트
          cp public/data/briefing.json public/data/latest.json
          echo "✅ Updated: public/data/latest.json"
          
          # dates.json 생성 (모든 날짜 목록)
          echo "📝 Generating dates.json..."
          echo '{"dates":[' > public/data/dates.json
          find public/data -type f -name "*.json" -path "*/[0-9][0-9][0-9][0-9]/[0-9][0-9]/*.json" | \
            sed 's|public/data/\([0-9]\{4\}\)/\([0-9]\{2\}\)/\([0-9]\{2\}\)\.json|\1-\2-\3|' | \
            sort -r | \
            sed 's/.*/"&"/' | \
            paste -sd ',' - >> public/data/dates.json
          echo ']}' >> public/data/dates.json
          
          # 파일 크기 확인
          echo "📊 File sizes:"
          ls -lh public/data/latest.json
          ls -lh public/data/dates.json
          ls -lh public/data/$YEAR/$MONTH/$DAY.json
          
          # dates.json 내용 미리보기
          echo "📋 dates.json preview:"
          head -n 5 public/data/dates.json
      
      - name: Create Pull Request
        run: |
          DATE=$(TZ=Asia/Seoul date +%Y-%m-%d)
          BRANCH_NAME="briefing/$DATE"

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          # 변경사항이 있는지 확인
          git add public/data/
          if git diff --staged --quiet; then
            echo "ℹ️ No changes to commit"
            exit 0
          fi

          # 새 브랜치 생성 및 체크아웃
          git checkout -b "$BRANCH_NAME"

          # 변경사항 커밋
          git commit -m "chore: update daily briefing [$DATE]"

          # 브랜치 푸시
          git push -u origin "$BRANCH_NAME"

          # PR 생성 (gh CLI 사용)
          PR_URL=$(gh pr create \
            --title "🤖 Daily AI Briefing Update - $DATE" \
            --body "## 📅 Daily AI Briefing - $DATE

          이 PR은 자동으로 생성된 일일 AI 브리핑 업데이트입니다.

          ### 📊 변경 사항
          - \`public/data/latest.json\` 업데이트
          - \`public/data/$DATE.json\` 생성
          - \`public/data/dates.json\` 업데이트

          ### 🔍 데이터 소스
          - GitHub Trending (실제 API)
          - Hacker News (AI 관련 뉴스)
          - YouTube (유명 AI 채널)

          ### ✅ 검증
          - [x] URL 유효성 검증 완료
          - [x] GitHub 레포지토리 존재 확인
          - [x] YouTube 비디오 링크 검증

          ---
          🤖 Generated by Daily Briefing Workflow" \
            --base main \
            --head "$BRANCH_NAME" \
            --label "auto-briefing")

          echo "✅ Pull Request created: $PR_URL"

          # PR 번호 추출
          PR_NUMBER=$(echo "$PR_URL" | grep -o '[0-9]*$')

          # PR 자동 승인 (GitHub Actions가 자신의 PR을 승인할 수 없으므로 별도 워크플로우에서 처리)
          # 이 라벨을 보고 별도 워크플로우가 자동 approve 처리함

          # Auto-merge 활성화 (승인 후 자동 병합되도록 미리 설정)
          gh pr merge "$PR_URL" --auto --squash

          echo "✅ Auto-merge enabled (승인 후 자동 병합 예정)"
          echo "📅 Date: $DATE"
          echo "🔀 Branch: $BRANCH_NAME"
          echo "🔗 PR URL: $PR_URL"
          echo "🏷️ Label: auto-briefing"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
